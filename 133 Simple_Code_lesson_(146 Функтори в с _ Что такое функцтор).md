### 📘 Функтор у C++

У C++ **функтор (functor)** — це об’єкт, який можна викликати як функцію. Тобто це клас або структура, що перевантажує оператор `operator()`.  

#### 🔑 Основні моменти:
- **Функтор = функціональний об’єкт**  
  Це звичайний об’єкт класу, але він поводиться як функція.
- **Перевантаження `operator()`**  
  Ключова ідея: ми визначаємо, що має робити об’єкт при виклику з дужками.
- **Гнучкість**  
  На відміну від звичайних функцій, функтори можуть зберігати стан (наприклад, параметри, лічильники).
- **Використання**  
  Часто застосовуються в алгоритмах стандартної бібліотеки (STL), наприклад у `std::sort`, `std::for_each`.

---

### 🛠 Приклад

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

struct MultiplyBy {
    int factor;
    MultiplyBy(int f) : factor(f) {}
    
    // Перевантаження operator()
    int operator()(int x) const {
        return x * factor;
    }
};

int main() {
    std::vector<int> v = {1, 2, 3, 4};
    MultiplyBy times2(2); // створюємо функтор
    
    std::transform(v.begin(), v.end(), v.begin(), times2);
    
    for (int n : v) {
        std::cout << n << " "; // Виведе: 2 4 6 8
    }
}
```

---

### 📊 Порівняння з іншими підходами

| Підхід            | Особливості |
|-------------------|-------------|
| **Звичайна функція** | Не має стану, просто виконує дію |
| **Функтор**         | Може зберігати дані, викликати як функцію |
| **Lambda**          | Анонімна функція, часто використовується замість простих функторів |

---

👉 Отже, функтор у C++ — це спосіб зробити об’єкт "функцією", але з можливістю мати внутрішній стан.  
Хочеш, я покажу приклад, де функтор використовується для сортування зі спеціальним критерієм?
