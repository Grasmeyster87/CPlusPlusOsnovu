**Краткий ответ:**  
`std::vector` в C++ — это контейнер из библиотеки стандартных шаблонов (STL), который реализует *динамический массив*. Он автоматически изменяет размер при добавлении или удалении элементов и обеспечивает быстрый доступ к элементам по индексу.  

---

## 🔹 Основные характеристики `std::vector`
- **Динамический размер**: автоматически увеличивает или уменьшает вместимость при добавлении/удалении элементов.  
- **Непрерывное хранение**: элементы хранятся в памяти последовательно, как в обычном массиве.  
- **Быстрый доступ**: операция доступа по индексу выполняется за \(O(1)\).  
- **Перемещение памяти**: при переполнении текущей ёмкости создаётся новый массив и все элементы копируются — это может быть затратным.  
- **Совместимость с итераторами**: поддерживает итераторы, что делает его удобным для работы с алгоритмами STL.  

---

## 🔹 Основные операции
- **Создание и инициализация**:
  ```cpp
  #include <vector>
  using namespace std;

  vector<int> v1;            // пустой вектор
  vector<int> v2(3, 5);      // {5, 5, 5}
  vector<int> v3 = {1, 2, 3}; // {1, 2, 3}
  ```
- **Добавление элементов**:
  ```cpp
  v1.push_back(10);   // добавить элемент в конец
  v1.emplace_back(20); // более эффективное добавление
  ```
- **Удаление элементов**:
  ```cpp
  v1.pop_back();      // удалить последний элемент
  v1.erase(v1.begin()); // удалить первый элемент
  ```
- **Доступ к элементам**:
  ```cpp
  int x = v1[0];      // без проверки границ
  int y = v1.at(0);   // с проверкой границ
  ```
- **Размер и ёмкость**:
  ```cpp
  v1.size();      // количество элементов
  v1.capacity();  // текущая ёмкость
  v1.reserve(100); // заранее выделить память
  ```

---

## 🔹 Преимущества и недостатки
| Преимущества | Недостатки |
|--------------|------------|
| Быстрый доступ по индексу | Перемещение памяти при росте |
| Простота использования | Удаление из середины дорогостоящее |
| Интеграция с алгоритмами STL | Может занимать больше памяти, чем массив |

---

## 🔹 Когда использовать `std::vector`
- ✅ Когда нужен массив переменной длины.  
- ✅ Когда важен быстрый доступ по индексу.  
- ❌ Если часто удаляются/вставляются элементы в середину — лучше использовать `std::list` или `std::deque`.  
