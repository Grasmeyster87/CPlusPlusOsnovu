**Предикат у C++ — це функція або об’єкт-функція, яка повертає булеве значення (`true` або `false`) і використовується для перевірки певної умови.**  

---

## 🔎 Детальніше про предикати

### 1. Загальне визначення
- У логіці предикат — це **булева функція** \(P: X \to \{0,1\}\), яка визначає істинність або хибність твердження для заданих аргументів.  
- У C++ предикат — це **будь-яка функція, лямбда або функціональний об’єкт**, що повертає `bool` і використовується в алгоритмах стандартної бібліотеки (STL).

### 2. Приклади предикатів
- **Функція-предикат:**
  ```cpp
  bool isEven(int x) {
      return x % 2 == 0;
  }
  ```
  Використання:
  ```cpp
  std::vector<int> v = {1, 2, 3, 4};
  auto it = std::find_if(v.begin(), v.end(), isEven);
  // знайде перший парний елемент
  ```

- **Лямбда-предикат:**
  ```cpp
  std::sort(v.begin(), v.end(), [](int a, int b) {
      return a > b; // сортування за спаданням
  });
  ```

- **Функтор-предикат:**
  ```cpp
  struct GreaterThanFive {
      bool operator()(int x) const {
          return x > 5;
      }
  };
  std::count_if(v.begin(), v.end(), GreaterThanFive());
  ```

### 3. Де застосовуються предикати
- **Алгоритми STL:**  
  - `std::sort` — приймає предикат для визначення порядку сортування.  
  - `std::find_if` — шукає елемент, що задовольняє предикат.  
  - `std::count_if` — рахує кількість елементів, що відповідають умові.  
  - `std::remove_if` — видаляє елементи, які задовольняють предикат.  

- **C++20 Concepts:**  
  У нових стандартах предикати формалізовані як концепти (`std::predicate`), що описують вимоги до функцій, які можна використовувати як умови.

### 4. Важливі особливості
- **Тип повернення:** завжди `bool`.  
- **Гнучкість:** може бути звичайною функцією, лямбдою або класом із перевантаженим оператором `()`.  
- **Призначення:** робить алгоритми універсальними, дозволяючи користувачу визначати власні правила перевірки чи сортування.

---

✅ Отже, **предикати в C++ — це інструмент для вираження умов у вигляді функцій, які повертають істину або хибність, і широко застосовуються в алгоритмах стандартної бібліотеки.**

Хочете, я покажу кілька практичних прикладів використання предикатів у реальних задачах — наприклад, фільтрація даних чи сортування структур?   

Sources: [Stack Overflow: як працює предикат](https://ru.stackoverflow.com/questions/479559/%d0%9a%d0%b0%d0%ba-%d1%83%d1%81%d1%82%d1%80%d0%be%d0%b5%d0%bd-%d0%b8-%d1%80%d0%b0%d0%b1%d0%be%d1%82%d0%b0%d0%b5%d1%82-%d0%bf%d1%80%d0%b5%d0%b4%d0%b8%d0%ba%d0%b0%d1%82) [Вікіпедія: предикат](https://uk.wikipedia.org/wiki/%D0%9F%D1%80%D0%B5%D0%B4%D0%B8%D0%BA%D0%B0%D1%82) [cppreference: std::predicate](https://en.cppreference.com/w/cpp/concepts/predicate)
